# Stack Configuration

**Detected:** markdown
**Auto-detected:** false
**Generated by:** init-agents (another-agent-skills)

## Commands

| Action | Command |
|---|---|
| Test | `bash scripts/skill-lint.sh skills/*/` |
| Lint | `bash scripts/skill-lint.sh skills/*/` |
| Type check | `echo "N/A — markdown project, no type system"` |
| Build | `echo "N/A — markdown project, no build step"` |
| Dev | `echo "N/A — see docs/ for landing page dev server"` |

## Lockfile

No lockfile detected (markdown-only project).

## How to Configure

If any command shows `<configure: ...>`, edit this file and replace with your actual command.
Skills (git-workflow, test-driven, etc.) read this file for project-specific behavior.

## Re-detect

After changing your project setup, re-run:
```bash
rm STACK_CONFIG.md && bash init-agents.sh
```
